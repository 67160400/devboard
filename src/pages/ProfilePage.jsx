import UserList from "../components/UserList"; // นำเข้าส่วนแสดงรายชื่อสมาชิก (ที่เราเขียนดึง API ไว้แล้ว)

function ProfilePage() {
  // สร้างหน้า Page สำหรับแสดงข้อมูลสมาชิกทั้งหมด

  return (
    <div
      style={{
        maxWidth: "500px", // จำกัดความกว้างหน้าจอนี้ไม่ให้เกิน 500px (ให้ดูเป็นระเบียบ)
        margin: "2rem auto", // จัดให้กล่องอยู่กึ่งกลางหน้าจอ (บน-ล่างห่าง 2rem, ซ้าย-ขวาปรับอัตโนมัติ)
        padding: "0 1rem", // เว้นระยะห่างขอบด้านข้างนิดหน่อยกันตัวหนังสือติดขอบจอ
      }}
    >
      {/* เรียกใช้งานคอมโพเนนต์ UserList เพื่อดึงข้อมูลและวาดรายชื่อสมาชิกออกมา */}
      <UserList />
    </div>
  );
}

export default ProfilePage; // ส่งออกหน้านี้ไปใช้งานในระบบ Router (App.jsx)
